# Option references non-existent insight
version: "1.0"
name: "Test"
description: "Test problem"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  method:
    label: "Method"
    type: method
    options:
      a:
        label: "A"
        insights:
          - nonexistent_insight
