# when field references a nonexistent decision
version: "1.0"
name: "Test"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  method:
    label: "Method"
    options:
      a:
        label: "A"
  conditional:
    label: "Conditional"
    when: nonexistent.a
    options:
      x:
        label: "X"
