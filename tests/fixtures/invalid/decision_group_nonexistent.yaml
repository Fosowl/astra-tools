# Decision group references a nonexistent decision
version: "1.0"
name: "Test"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  method:
    label: "Method"
    type: method
    options:
      a:
        label: "A"
decision_groups:
  - label: "Group 1"
    decisions:
      - method
      - nonexistent_decision
