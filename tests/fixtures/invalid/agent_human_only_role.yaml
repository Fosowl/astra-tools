# An agent attributed a human-only role (conceptualization).
version: "0.0.13"
name: "Test"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
actors:
  assistant:
    type: agent
    model: claude-opus-5
decisions:
  method:
    label: "Method"
    options:
      a:
        label: "A"
        proposed_by: {actor: assistant, role: conceptualization}
