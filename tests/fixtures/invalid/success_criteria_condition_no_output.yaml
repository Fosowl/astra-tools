version: "0.1"
name: "Invalid"
description: "Condition without output"
success_criteria:
  - claim: "Should fail"
    condition: "value > 0.95"
inputs:
  - id: raw_data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  method:
    label: "Method"
    default: a
    options:
      a:
        label: "A"
