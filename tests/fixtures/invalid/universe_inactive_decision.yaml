# Universe specifies a conditional decision whose condition is not met.
# svm_kernel has when: model.svm, but model is set to rf.
id: bad_inactive
description: "Specifies inactive conditional decision"

decisions:
  preprocessing: standard
  model: rf
  svm_kernel: rbf
  test_split: split_20
  seed: seed_42
