# Both flat decisions and decision_groups (invalid)
version: "1.0"
name: "Test"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  method:
    label: "Method"
    type: method
    default: a
    options:
      a:
        label: "A"
decision_groups:
  - label: "Group 1"
    decisions:
      other:
        label: "Other"
        type: method
        default: x
        options:
          x:
            label: "X"
