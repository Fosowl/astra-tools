version: "1.0"

analysis:
  name: "Bad From Ref Test"
  problem: "Test invalid 'from' reference on output"

  inputs:
    - id: data
      type: data

  outputs:
    - id: result
      type: figure
      formats: [png]
      from: nonexistent_phase.some_output

phases:
  step_a:
    problem: "Step A"
    inputs:
      - id: data
        from: inputs.data
    outputs:
      - id: output
        type: data
