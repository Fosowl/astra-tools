version: "1.0"

analysis:
  name: "Self Reference Test"
  problem: "Test self-referencing phase"

  inputs:
    - id: data
      type: data

  outputs:
    - id: result
      type: metric
      dtype: float

phases:
  step_a:
    problem: "Step A"
    inputs:
      - id: data
        from: step_a.output
    outputs:
      - id: output
        type: data
